import React from 'react';

function debounce(func: Function, wait: number) {
  // should wait according to wait param
  // to call the function
  // setTimeout(func(), wait);

  // clearTimeout to prevent the setTimeout to execute
  // clearTimeout()
}